import { HomeClient } from "./page-client";

/**
 * Home/Dashboard Page
 * Server component that renders client-side logic for authentication and dashboard
 * SEO metadata is inherited from layout.tsx with template title
 */
const Home = () => {
    return <HomeClient />;
};

export default Home;
